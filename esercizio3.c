//Esercizio 3 – Rettangolo
//Fatto inserire all’utente le dimensioni di un rettangolo
//scrivere un programma che ne calcoli e restituisca all’utente 
//il volume e l’area delle superfici.

#include <stdio.h>

int main() {
    int perimetro, altezza, base, area;

    printf("inserisci le dimensioni di cui vuoi calcolare il perimetro e l'area: ")
    scanf("%d", &base);
    scanf("%d", &altezza);
    area= base * altezza 
    perimetro = (base + altezza) * 2;

    printf("l'area del rettangolo è %d e il perimetro è %d, area, perimetro");
    


}